import styled from 'styled-components'
import { Button } from './button'

const backgroundColor = '#2e87ec'
const color = '#ffffff'

export const BlueButton = styled(Button)`
  background-color: ${backgroundColor};
  color: ${color};
`

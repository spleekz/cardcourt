import React from 'react'
import styled from 'styled-components'

const PlayCheckContainer = styled.div`
  font-size: 30px;
`

export const PlayCheck: React.FC = (): JSX.Element => {
  return <PlayCheckContainer>Play Check</PlayCheckContainer>
}
